/* Control-flow:
*
* 0. 
*
*
* 1. 
*
* 2. 
*/

/*If
* - when you start a condtional statement, you use 'if'
*Else-if
* - to chain any 'if' statements, you use 'else-if', unless you are adding a 
* default
*Else
* - the default statemnt, to end the chain
*/

//Switch//
/*
*A switch statement is a conditional that evaluates an expression and matches
* the value of the expression with a 'case' clause. A break is the default used
to end the chain of case clauses. Failure to add a break at the end of the block
will cause the next case block to execute. 
*/